version: "2"

formatters:
  enable:
    - goimports
    - golines
    - gofmt

  settings:
    goimports:
      local-prefixes:
        - github.com/philipkrueck/adventofcode
    golines:
      max-len: 120
